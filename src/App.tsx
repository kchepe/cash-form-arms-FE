import React, { type ReactNode } from 'react';
import { TextField } from './common';

const App = (): ReactNode => {
  return (
    <div>
      <TextField label="Text" />
    </div>
  );
};

export default App;
